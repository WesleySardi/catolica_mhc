class Student {
  String? degree, email, name, phone, password;

  //Student({degree, email, name, phone, password});
  Student({required degree, required email, required name, required phone, required password});
  // Criar a funções pra pegar esses dados e depois passar pro perfil

}